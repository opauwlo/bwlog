<link rel="stylesheet" href="/css/content-styles.css">
<link rel="stylesheet" href="/css/gist.css">
<link rel="stylesheet" href="/css/prism.css">
{{#each posts}}

  <div class="bg rounded-3 mt-1 mb-2">
    <section class=" mx-2 mt-3 w-auto rounded-pill shadow">
      <div class="d-flex align-items-center my-2 text-light ">
        <div class="flex-shrink-0">
          <img class="rounded-circle w-50 border border-secondary"src="{{dataValues.foto}}" alt="profile picture">
        </div>
        <div class="flex-grow-0 float-start text-profile">
          <a class="text-white-50 fs-6" href="/autor/{{dataValues.id_user}}">
            {{dataValues.autor}}
          </a>
        </div>
      </div>
    </section>
    <div class="card-body">
      <div class="text-white"> 
        <h1 class="mb-3 ">{{dataValues.titulo}}</h1>
      </div>
      <hr class="text-white-50 shadow-lg">
      <section class="text-white-50 ck-content">
        <article class="ck-content">
          {{{dataValues.conteudo}}}
          
        </article>
      </section>
      <p class="text-secondary mt-3 fst-italic fs-6 text-end">
        publicado em: {{#formatDate dataValues.createdAt}}{{/formatDate}}
      </p>
      {{#if dataValues.editado}}
        <small class="fw-light fst-normal text-blue mx-1">
          post editado
        </small>
      {{/if}}
    </div>
  </div>

  <div class=" my-2 card bg text-light border-dark rounded rounded-2">
    <div class="card-body bg">
      <h5 class="card-title">Discuta sobre o texto no fórum</h5>
      <p class="card-text">Compartilhe o que pensou :)</p>
      <a href="/forum/{{dataValues.id}}/{{dataValues.titulo}}" class="btn btn-primary">Participar</a>
    </div>
  </div>

{{else}}
{{> _404}}
{{/each}}
{{> _loadingPage}}
<script src="/js/prism.js"></script>


